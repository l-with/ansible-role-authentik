---

authentik_image_tag:            2024.6.1
authentik_error_reporting:      false
authentik_base_directory:       /opt/authentik
# authentik_pg_pass:
# authentik_secret_key

authentik_bootstrap: true
# authentik_bootstrap_password:
# authentik_bootstrap_token:

# authentik_smtp_host:
# authentik_smtp_port:
# authentik_smtp_username:
# authentik_smtp_password:
# authentik_smtp_tls:
# authentik_smtp_ssl:
# authentik_smtp_from:

...
